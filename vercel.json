{
    "version": 2,
    "builds": [
        {
            "src": "client/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        },
        {
            "src": "api/scan.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/scan",
            "dest": "/api/scan.js"
        },
        {
            "src": "/(.*)",
            "dest": "/client/$1"
        }
    ]
}